<form method="post" action="{{ path('app_commande_delete', {'Reference': commande.Reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.Reference) }}">
    <button class="btn">Delete</button>
</form>
